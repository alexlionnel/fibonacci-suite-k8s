### Commande pour appliquer tous les fichiers de config kubernetes dans le répertoire k8s
```kubernetes
kubectl apply -f k8s
```

### Activer Ingress avec minikube
```
minikube addons enable ingress
```